# scratchuser

**scratchuser** es una librería de Python que permite obtener información pública de usuarios de [Scratch](https://scratch.mit.edu) utilizando la API oficial (`https://api.scratch.mit.edu`). Puedes ver datos como la biografía, el país, la imagen de perfil, seguidores, proyectos y más.

---

## 🔧 Instalación

Primero, asegúrate de tener Python 3.6 o superior. Luego puedes instalar la librería (una vez publicada en PyPI) con:

```bash
pip install scratchuser
